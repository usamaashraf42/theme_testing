
{% if textVersion == 1 %} 
  {% assign ticketText = 'GET 25% OFF'%}
{% else %}
  {% assign ticketText = '48-HOUR VIP ACCESS'%}
{% endif %}

<div class="ticker-wrap" style="border: 1px solid {{ main_colour }}">
  <div class="ticker">
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
    <div class="ticker__item" style="color: {{ main_colour }}">.</div>
    <div class="ticker__item" style="color: {{ main_colour }}">{{ ticketText }}</div>
  </div>
</div>

<style>

@-webkit-keyframes ticker {
  0% {
    -webkit-transform: translate3d(0, 0, 0);
    transform: translate3d(0, 0, 0);
    visibility: visible;
  }

  100% {
    -webkit-transform: translate3d(-100%, 0, 0);
    transform: translate3d(-100%, 0, 0);
  }
}

@keyframes ticker {
  0% {
    -webkit-transform: translate3d(0, 0, 0);
    transform: translate3d(0, 0, 0);
    visibility: visible;
  }

  100% {
    -webkit-transform: translate3d(-100%, 0, 0);
    transform: translate3d(-100%, 0, 0);
  }
}

.ticker-wrap {
  max-width: 85vw;
  margin: auto;
  overflow: hidden;
  height: 4rem;
  border: 1px solid #000000;
  border-radius: 25px;
  padding: 5px;
  padding-top: 12px;
  box-sizing: content-box;
  margin-left: 20px;
}

@media (min-width: 769px) {
  .ticker-wrap {
    max-width: 350px;
    margin-left: 38vw
  }
}

.ticker {

display: inline-block;
height: 4rem;
line-height: 4rem;  
white-space: nowrap;
padding-right: 100%;
box-sizing: content-box;

-webkit-animation-iteration-count: infinite; 
        animation-iteration-count: infinite;
-webkit-animation-timing-function: linear;
        animation-timing-function: linear;
-webkit-animation-name: ticker;
       animation-name: ticker;
-webkit-animation-duration: 18000ms;
        animation-duration: 18000ms;

}

.ticker__item {

display: inline-block;
padding: 0 2rem;
font-size: 2rem; 
color: #000000;
font-family: "Manifold";
font-size: 24px;
font-weight: 500;
letter-spacing: 0.65px;
line-height: 17px; 

}

</style>